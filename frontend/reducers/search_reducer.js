

const SearchBarReducer = (state = {}, action) => {
    Object.freeze(state);
    switch(action.type){
        case TESTER:
            return Random
        default:
            return state
    }

}